# bizmote 請求書管理システム 運用マニュアル(月次フロー)

このマニュアルは **毎月の運用手順** を説明します。
初期セットアップは `SETUP.md` を参照してください。

---

## 月次フロー全体像

```
[月初1日 09:00]      自動: 6社分の請求行が自動作成される(ステータス: 未入力)
        ↓
[月初〜月末まで]      オーナー: 入力フォームから金額入力 → 提出 (→ 入力済)
        ↓
[月末]              経理: 承認画面で確認 → 承認 (→ 承認済)
        ↓
[月末〜翌月初日]      経理: 一括発行 → freee に請求書発行 (→ 発行済)
        ↓
[翌月初日]          経理: PDFプレビュー → 一括メール送付 (→ 送付済)
        ↓
[翌月末まで]         取引先からの入金待ち
        ↓
[翌月15日 09:00]     自動: 入金消込チェック → 入金確認できたら 入金済
                            未入金なら Slack 通知
```

---

## 役割

| 役割 | 担当者 | 月次の主な作業 |
|---|---|---|
| **オーナー** | 山岡(daisuke.yamaoka@bizmote.jp)<br>須藤(hayato.suto@bizmote.jp) | 担当案件の請求金額を入力フォームから提出 |
| **経理** | 樋口(miku.higuchi@bizmote.jp)<br>山岡(代行可能) | 承認 → 発行 → メール送付 → 入金確認 |

---

## オーナー作業: 請求金額の入力(月末まで)

### 1. オーナー入力フォームを開く

1. スプレッドシート `bizmote_請求管理シート` を開く
2. メニュー「請求管理 → **オーナー → 入力フォームを開く**」をクリック
3. 画面右にサイドバーが開く
4. 自分が担当する未入力 / 差戻 案件のみが表示される

### 2. 金額を入力

各カードで以下を確認・編集:

- **品目名**: `02_品目テンプレート` から自動で初期表示される
- **単価(税抜)**: 必要に応じて修正
- **数量**: 必要に応じて修正
- 「+ 行を追加」で品目追加、「×」で削除
- **備考**: freee請求書の備考欄に転記される(例: 「振込手数料はご負担ください」)

入力中は小計(税抜)と税込金額がリアルタイムに表示されます。

### 3. 前月コピー(任意)

「前月コピー」ボタンで前月の品目+備考をワンクリックで取り込めます。固定額の案件で楽。

### 4. 提出

「提出」ボタンをクリック → カードが消えて、`03_請求一覧` のステータスが **入力済** に更新されます。

---

## 経理作業: 承認 → 発行 → 送付(月末〜翌月初日)

### 1. 承認画面を開く

1. メニュー「請求管理 → **経理 → 承認画面を開く**」
2. 画面中央にモーダル(承認画面)が開く
3. 「入力済」ステータスの請求書が一覧表示される

### 2. 内容確認

各カードで:

- 取引先名 / 対象月 / 件名
- 入力者・入力日時
- 税込金額
- **前月比**(±20% 超は赤色警告)
- 「明細」ボタンで品目詳細を展開
- 「**備考**」(印刷される)が黄色で常時表示

### 3. 承認 or 差戻

- **承認**: 内容OKなら「承認」ボタン → ステータス「**承認済**」
- **差戻**: 修正が必要なら「差戻」ボタン → 理由入力 → ステータス「**差戻**」
- **一括承認**: 全件まとめて承認したい場合は右上の「一括承認」

差戻の場合、オーナーが入力フォームから再入力(備考も継承)→ 再提出。

### 4. 発行プレビュー(任意)

メニュー「請求管理 → **経理 → 発行プレビュー(承認済の確認)**」

freee に送る予定の内容(税込金額・期日・件名・取引先) が一覧で出ます。**freee API は呼ばれません**(安全)。

### 5. 一括発行(本番)

メニュー「請求管理 → **経理 → 一括発行(本番・freee連携)**」

確認ダイアログで件数+合計金額を確認 → 「はい」→ freee に請求書発行

成功すると:
- ステータス「**発行済**」
- `freee請求書ID` 列に番号が記録される
- freee 管理画面に請求書が作成される(請求日: 今日 / 期日: 翌月末)

### 6. PDFプレビュー(任意・推奨)

メール送付前に PDF の見た目を確認:

1. `03_請求一覧` シートで対象行(発行済)をクリック
2. メニュー「経理 → **選択行のPDFをプレビュー**」
3. モーダルで請求書のレイアウトを目視確認

### 7. 一括メール送付

メニュー「請求管理 → **経理 → 一括メール送付(本番)**」

確認ダイアログ:
- 差出人(スクリプト実行者のアカウント)
- 送信可能件数
- 送付対象一覧

→「はい」で取引先に PDF添付メールを一括送信

成功すると:
- ステータス「**送付済**」
- 送付完了日時が記録
- 取引先には bizmote のメールから PDF添付メールが届く
- 社内CCに山岡(or 樋口)が入っていればbizmote側でも受信確認可

### 8. (もし手動でメール送付した場合)送付済マーク

freee UI で手動メール送付した場合、スプレッドシート側のステータスを揃えるため:

1. `03_請求一覧` で該当行をクリック
2. メニュー「経理 → **選択行を送付済にする(手動マーク)**」

---

## 月中作業: 入金消込チェック(自動・任意手動)

### 自動

毎月15日 9:00 JST に `入金消込トリガー` が動き、freee の入金状況をチェックします:

- **入金確認できた請求** → ステータス「**入金済**」+ Slack に通知
- **期日超過の未入金** → ステータス「**未消込警告**」+ Slack に警告通知

### 手動

任意のタイミングで実行したい場合:

メニュー「請求管理 → **経理 → 入金消込チェック(手動)**」

---

## 例外対応

### 新規クライアント追加

1. **先に freee 管理画面で取引先を作成**
2. その freee取引先ID(数値)を控える
3. メニュー「管理 → **freee取引先IDから新規クライアント追加**」
4. プロンプト1: freee取引先ID
5. プロンプト2: クライアントID(例: `clt-newco`)
6. `01_クライアントマスタ` に新規行が追加される
7. 行の以下を手動で埋める:
   - 事業分野 / 案件オーナー / To担当者名 / Toアドレス / CCアドレス / 件名テンプレ
8. `02_品目テンプレート` に該当クライアントの品目雛形を追加

### クライアント停止(契約終了など)

`01_クライアントマスタ` の該当行で **ステータス列を「停止」** に書き換えるだけ。
次月からは月初トリガーで請求行が作成されなくなります。

### freee 側で取引先情報が変わった

メニュー「管理 → **クライアント情報を freee と同期**」で全クライアントの登録名・住所を一括更新できます。

### 月初トリガーが動かなかった(請求行が作られない)

メニュー「管理 → **月初の請求行を作成(手動)**」で手動実行できます。重複防止機構あり。

### データを最初からやり直したい(月初〜中旬の場合のみ)

メニュー「管理 → **請求データをリセット(復旧用)**」

`03_請求一覧` `03b_請求明細` のデータ行を全削除して、当月の6行を再作成します。
**既に発行済の請求書がある場合は使わない**(freee側との不整合になるため)。

### Slack通知が来ない

1. メニュー「開発者 → 7. Slack通知テスト」で疎通確認
2. URL が間違っていれば「開発者 → 5. Slack Webhook URLを設定」で再登録

### freee 認証が切れた(6時間以上空いた)

通常は OAuth2ライブラリで自動更新されます。失敗時は:
1. メニュー「開発者 → 3. freee認証状態確認」で確認
2. 「未認証」なら「2. freee認証開始」で再認証

---

## ステータス一覧と意味

| ステータス | 意味 | 次のアクション |
|---|---|---|
| **未入力** | 月初に自動作成された初期状態 | オーナーが入力フォームから入力 |
| **入力済** | オーナー入力完了 | 経理が承認 |
| **差戻** | 経理が差し戻し | オーナーが再入力 |
| **承認済** | 経理承認完了 | 一括発行で freee へ |
| **発行済** | freee 請求書作成・取引登録完了 | PDFプレビュー → メール送付 |
| **送付済** | メール送付完了 | 入金待ち(自動消込で次のステータスへ) |
| **入金済** | 入金消込完了 | 終端 |
| **未消込警告** | 期日超過で未入金 | 樋口/山岡が取引先に督促 |

---

## メニュー早見表

| 役割 | メニュー | いつ使うか |
|---|---|---|
| オーナー | 入力フォームを開く | 月末までに金額入力 |
| 経理 | 承認画面を開く | 月末・経理確認 |
| 経理 | 発行プレビュー | 一括発行前の最終確認(任意) |
| 経理 | 一括発行(本番) | 経理確認後 |
| 経理 | 選択行のPDFをプレビュー | 送付前の見た目確認 |
| 経理 | 一括メール送付(本番) | 取引先送付 |
| 経理 | 選択行を送付済にする | freee UI で手動送付した時 |
| 経理 | 入金消込チェック(手動) | 任意タイミングで状況確認 |
| 管理 | 月初の請求行を作成(手動) | トリガー失敗時の救済 |
| 管理 | 請求データをリセット | テスト・復旧用 |
| 管理 | 月初/入金消込トリガー登録 | 初期セットアップで1回 |
| 管理 | クライアント freee同期 | 取引先情報変更時 |
| 管理 | 新規クライアント追加 | 新規取引先増えた時 |

---

## 困ったら

- エラーメッセージのスクリーンショットを保存
- メニュー「開発者 → 3. freee認証状態確認」「6. freee接続テスト」で疎通確認
- Apps Script エディタの「実行ログ」(時計アイコン)で詳細ログを確認
